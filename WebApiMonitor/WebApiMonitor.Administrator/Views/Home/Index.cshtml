@model List<DashboardItem>

@{
    ViewBag.Title = "Dashboard";
}

@section scripts {

    <script>
        function onDashboardLoaded() {
            ////$(".dashboard-frame").contents().find(".sidebar").attr("style", "display:none;");

            //var frm = frames[0].document;
            //var otherhead = frm.getElementsByTagName("head")[0];
            //var link = frm.createElement("link");
            //link.setAttribute("rel", "stylesheet");
            //link.setAttribute("type", "text/css");
            //link.setAttribute("href", "Content/site.css");
            //otherhead.appendChild(link);
            console.log('iframe loaded');
        }
    </script>

}

@*<h2 class="page-header">@ViewBag.Title (<a href="~/Home/Edit">ред</a>/<a re href="@ViewBag.GrafanaUrl" target="_blank">grafana <span class="glyphicon glyphicon-log-out" style="font-size: 18px;"></span></a>)</h2>
    <h3>@ViewBag.Message</h3>

    @foreach (var item in Model)
    {
        <iframe src="@item.Url" width="450" height="220" frameborder="0"></iframe>
    }*@

<iframe src="http://localhost:3001/d/23jK1dNmz/mashina?orgId=1&from=now-5m&to=now&theme=light" class="dashboard-frame" onload="onDashboardLoaded(this)"
        id="dashboard-frame"></iframe>