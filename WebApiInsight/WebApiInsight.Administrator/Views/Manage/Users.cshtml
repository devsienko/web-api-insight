@model WebApiInsight.Administrator.Models.UsersViewModel
@using Microsoft.Owin.Security
@{
    ViewBag.Title = "Manage your external logins";
}

<h2>@ViewBag.Title.</h2>

<p class="text-success">@ViewBag.StatusMessage</p>
@{
    if (Model.Users.Count > 0)
    {
        <h4>Registered Logins</h4>
            <table class="table">
                <tbody>
                    @foreach (var login in Model.Users)
                    {
                        <tr>
                            <td>@login.UserName</td>
                            <td>
                                @using (Html.BeginForm("RemoveLogin", "Manage"))
                                {
                                @Html.AntiForgeryToken()
                                <div>
                                    <input type="submit" class="btn btn-default" value="Remove" title="Remove this LoginProvider login from your account" />
                                </div>
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
    }
    else 
    {
        <span>Список пользователей пуст</span>
    }
}
